apiVersion: v1
kind: Service
metadata:
  name: patchproof
spec:
  type: "{{ .Values.service.type }}"
  selector:
    app: patchproof
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 4173
